{# filepath: c:\Users\ariel\Documents\curso\templates\venta\listar.html.twig #}
{% extends 'base.html.twig' %}
{% block contenido %}
	<h1>Lista de Ventas</h1>
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Fecha</th>
                <th>Total</th>
                <th>Usuario</th>
                <th>Detalles</th>
            </tr>
        </thead>
        <tbody>
            {% for venta in ventas %}
                <tr>
                    <td>{{ venta.id }}</td>
                    <td>{{ venta.fecha|date('d/m/Y H:i') }}</td>
                    <td>{{ venta.total }}</td>
                    <td>{{ venta.usuario.email }}</td>
                    <td>
                        <ul>
                            {% for detalle in venta.detalles %}
                                <li>
                                    Producto: {{ detalle.producto }},
                                    Cantidad: {{ detalle.cantidad }},
                                    Precio unitario: {{ detalle.precioUnitario }},
                                    Subtotal: {{ detalle.subtotal }}
                                </li>
                            {% endfor %}
                        </ul>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">No hay ventas registradas.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
