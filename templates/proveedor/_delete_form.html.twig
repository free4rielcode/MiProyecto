<form method="post" action="{{ path('app_proveedor_delete', {'id': proveedor.id}) }}" onsubmit="return confirm('estas seguro que quieres eliminar este registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proveedor.id) }}">
    <button class="btn btn-primary text-light">Eliminar</button>
</form>
